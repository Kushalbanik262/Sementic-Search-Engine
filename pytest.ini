[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers --durations=5
markers =
    anyio: async test driven by the anyio pytest plugin
filterwarnings =
    ignore::DeprecationWarning
