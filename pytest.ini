[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers --durations=5
markers =
    anyio: async test driven by the anyio pytest plugin
    integration: needs a live RabbitMQ at RABBITMQ_URL; skipped when unreachable
filterwarnings =
    ignore::DeprecationWarning
